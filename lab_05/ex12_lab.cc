#include <iostream>


int main() {

    std::cout << "enter first number:";
    int x = 0;
    std::cin >> x;

    if (x > 0) {
        std::cout << "first number is bigger then zero\n";
    }

    std::cout << "enter second number:";
    int x = 0;
    std::cin >> x;

    if (x > 0) {
        std::cout << "second number is bigger then zero too\n";
    }

}
